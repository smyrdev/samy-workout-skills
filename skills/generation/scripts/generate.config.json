{
  "_comment": "Every dataset-independent number and default rule the generation algorithm uses. Retune here — never in generate.py, SKILL.md, or CLAUDE.md. Dataset-specific knowledge (field names, tiers, muscle mapping) lives in ../datasets.json instead. Prose rationale: docs/generation.md.",

  "default_rules": {
    "exclude": { "exercises": [], "equipment": [], "movement_groups": [], "muscles": [] },
    "focus": { "muscles": [], "must_include": [] },
    "order": [
      "must_include_first",
      "trailer_groups_last",
      "compound_before_isolation",
      "focus_muscles_first",
      "large_groups_before_small"
    ]
  },

  "split_sessions": {
    "full_body": {
      "pattern": ["full_body"],
      "groups": {
        "full_body": ["chest", "back", "shoulders", "biceps", "triceps",
                      "quads", "hamstrings", "glutes", "calves", "core"]
      }
    },
    "upper_lower": {
      "pattern": ["upper", "lower"],
      "groups": {
        "upper": ["chest", "back", "shoulders", "biceps", "triceps"],
        "lower": ["quads", "hamstrings", "glutes", "calves", "core"]
      }
    }
  },

  "focus_bonus": 0.5,
  "name_length_penalty": 0.02,
  "indirect_discount": 0.5,
  "short_tolerance_sets": 1.0,
  "compound_min_muscles": 3,
  "trailer_groups": ["core", "calves"],
  "_trailer_comment": "What the trailer_groups_last order rule means. Travels in the brief; the coach applies it.",

  "reps_by_goal": {
    "hypertrophy": { "compound": "8-12", "isolation": "10-15" },
    "strength": { "compound": "3-6", "isolation": "6-10" },
    "both": { "compound": "6-10", "isolation": "8-12" }
  },

  "deload_sets_multiplier": 0.5,

  "_brief_comment": "How many candidates each muscle group offers, and the rest interval per goal. The exercises-per-session ceiling is the volume block's and is passed through untouched.",

  "candidates_per_muscle": 8,
  "rest_seconds_by_goal": {
    "hypertrophy": { "compound": 150, "isolation": 90 },
    "strength": { "compound": 210, "isolation": 120 },
    "both": { "compound": 180, "isolation": 105 }
  },
  "_floor_comment": "The one training rule the generator enforces — coaching.md 1.4. Scoped to the goals that include muscle growth, exactly as that rule scopes itself.",

  "rep_floors": { "compound": 4, "isolation": 6 },
  "rep_floor_goals": ["hypertrophy", "both"]
}
